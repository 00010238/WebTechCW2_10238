extends layout.pug 

block content 
    section.blog 
        .container
            .headline
                h2.title= item.title
                .btns
                    a(href=`/blogs/update/${item.id}` method="get") Update 
                    a(href=`/blogs/${item.id}/delete`).del Delete       
        p.blog-content= item.content 
        